<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.abhigrith">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Abhigrith">
        <activity android:name=".Parent.ProfileActivity"></activity>
        <activity android:name=".Parent.ParentSelectorActivity" />
        <activity android:name=".Parent.ParentSelectionActivity" />
        <activity android:name=".Parent.ParentRegisterActivity" />
        <activity android:name=".Parent.ParentProfileActivity" />
        <activity android:name=".Parent.ParentDashboardActivity" />
        <activity android:name=".Parent.ParentLoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>